🌡️ Simple Temperature Converter

A basic, single-page web tool for converting temperature values between Celsius ($\text{}^\circ\text{C}$) and Fahrenheit ($\text{}^\circ\text{F}$).

✨ Key Features

Conversion: Converts temperature in both directions: Celsius $\leftrightarrow$ Fahrenheit.

Simple UI: Features clear input fields and dedicated buttons for each conversion.

Visuals: Uses a nature image (background.jpg) as the main header background.

Technology: Built with HTML and JavaScript (logic and presentation are in index.html).

📂 Files

index.html: Contains all the structure (HTML), styling (inline CSS), and the conversion logic (JavaScript).

background.jpg: The image used for the page background.

🚀 How to Run

Make sure index.html and background.jpg are in the same folder.

Double-click index.html to open it in your web browser.

Enter a number in one of the fields and click the corresponding "Convert" button.
